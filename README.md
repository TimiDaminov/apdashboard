# A&P Sales Dashboard

Интерактивный дэшборд (Streamlit) для визуализации План/Факт из Excel.

## Запуск локально
```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
streamlit run app.py
